# Debug Logging

To enable debug logging we need to handle different frameworks differently. The following sections describe how to enable debug logging for each framework.

## OpenCV

To enable debug logging for OpenCV we need to set the following environment variables:

```bash
export OPENCV_LOG_LEVEL=DEBUG
export OPENCV_VIDEOIO_DEBUG=1
```
